import React from "react";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import {test, expect} from "@jest/globals";
import NavBar from "./index.js";

// test if the home button renders
test("renders home button",  () => {
});

// test if the itinerary button renders
test("renders itinerary button",  () => {
});

// test if maps button renders
test("renders maps button",  () => {
});

// test if the favourites button renders
test("renders favourites button",  () => {
});

// test if home button is clickable
test("home button is clickable",  () => {
});

// test if itinerary button is clickable
test("itinerary button is clickable",  () => {
});

// test if maps button is clickable
test("maps button is clickable",  () => {
});

// test if favourites button is clickable
test("favourites button is clickable",  () => {
});

// test if home button redirects to home page
test("home button redirects to home page",  () => {
});

// test if itinerary button redirects to itinerary page
test("itinerary button redirects to itinerary page",  () => {
});

// test if maps button redirects to maps page
test("maps button redirects to maps page",  () => {
});

// test if favourites button redirects to favourites page
test("favourites button redirects to favourites page",  () => {
});

