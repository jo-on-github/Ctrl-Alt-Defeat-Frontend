import React from "react";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import {test, expect} from "@jest/globals";
import Homepage from "./index.js";


// test if the city name renders from user input
test("renders city name from user input",  () => {
})

// test if the city name renders from randomiser button
test("renders city name from randomiser button",  () => {
})

// test if the correct list items render from user input
test("renders correct list items from user input",  () => {
})

// test if the correct list items render from randomiser button
test("renders correct list items from randomiser button",  () => {
})

// test if the nav bar renders on page load 
test("renders nav bar",  () => {
})

// test if the header renders on page load 
test("renders header",  () => {
})